declare const global: {global: {}};
declare const process: {env: {NODE_ENV: 'production' | 'development'}};
// declare function setTimeout(callback: () => void, timer: number): number;
// declare function clearTimeout(timer: number): void;
